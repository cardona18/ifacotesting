<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <data>

        <menuitem id="requests_root_menu" name="Solicitudes" web_icon="requests_gi,static/description/icon.png"/>

        <menuitem id="config_main_menu" name="Configuración" parent="requests_root_menu" sequence="100"/>

        <!-- User request privileges -->

        <record id="partner_users_request_action" model="ir.actions.act_window">
            <field name="name">Usuarios</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.partner</field>
            <field name="view_type">form</field>
            <field name="view_mode">kanban,tree,form</field>
            <field name="domain">[('user_ids', '!=', False)]</field>
            <field name="view_id" eval="ref('base.res_partner_kanban_view')"/>
        </record>

        <menuitem id="partner_users_main_menu" name="Usuarios" action="partner_users_request_action" parent="config_main_menu" groups="group_requests_manage_request" sequence="10"/>

    </data>
</odoo>
